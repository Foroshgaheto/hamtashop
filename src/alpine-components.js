/**
 * Alpine.js component registrations (extend in later phases).
 */

export function registerAlpineComponents() {
  document.addEventListener('alpine:init', () => {
    // Components registered here in later phases.
  });
}
